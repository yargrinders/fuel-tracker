# Fuel Price Tracker

Этот проект представляет собой Telegram-бота и веб-панель для отслеживания цен на топливо. Он парсит данные с сайта `clever-tanken.de`, сохраняет их в локальной базе данных, уведомляет пользователей об изменениях цен и предоставляет аналитику.

## ✨ Возможности

- **Отслеживание цен:** Бот периодически проверяет цены на дизельное топливо, E5 и E10 на указанных заправочных станциях.
- **Telegram-бот:** Пользователи могут взаимодействовать с ботом для получения актуальных цен, установки целевых цен и просмотра аналитики.
- **Веб-панель:** Визуальный интерфейс для просмотра текущего состояния, статистики и собранных данных.
- **Уведомления:** Бот отправляет уведомления, когда цена достигает установленного пользователем целевого значения.
- **Аналитика:** Предоставляет информацию о лучшем времени для заправки на основе исторических данных за последнюю неделю.
- **Резервное копирование:** Автоматическое резервное копирование данных в Google Drive.
- **Темная/Светлая тема:** Веб-панель поддерживает переключение тем.

## 🚀 Технологический стек

- **Backend:** Node.js, Express
- **Telegram Bot:** `node-telegram-bot-api`
- **Web Scraping:** `axios`, `cheerio`
- **База данных:** JSON-файлы
- **Резервное копирование:** `googleapis`
- **Frontend:** HTML, CSS, JavaScript (без фреймворков)

## 🔧 Установка и настройка

1.  **Клонировать репозиторий:**
    ```bash
    git clone <repository-url>
    cd fuel-tracker
    ```

2.  **Установить зависимости:**
    ```bash
    npm install
    ```

3.  **Создать `.env` файл:**
    Создайте файл `.env` в корневой директории и заполните его необходимыми переменными окружения.

4.  **Запустить приложение:**
    ```bash
    node index.js
    ```

## ⚙️ Конфигурация

Следующие переменные окружения должны быть установлены в файле `.env`:

- `TELEGRAM_TOKEN`: Токен вашего Telegram-бота.
- `ADMIN_CHAT_IDS`: ID чатов администраторов (через запятую).
- `GDRIVE_KEYFILE`: Путь к файлу ключей сервисного аккаунта Google.
- `GDRIVE_DATABASE_ID`: ID файла `database.json` в Google Drive.
- `GDRIVE_USERS_ID`: ID файла `users.json` в Google Drive.
- `GDRIVE_STATIONS_ID`: ID файла `stations.json` в Google Drive.
- `PORT`: Порт для веб-сервера (по умолчанию 3000).
- `AUTO_RESTORE_ON_START`: Установите `1` для автоматического восстановления данных из Google Drive при запуске.

## 🤖 Команды Telegram-бота

- `/start`: Начать взаимодействие с ботом.
- `/help`: Показать подробную помощь по командам.
- `/prices`: Показать актуальные цены на всех станциях.
- `/analytics`: Показать анализ лучшего времени для заправки.
- `/stats`: Показать статистику базы данных.
- `/settarget <тип> <цена>`: Установить целевую цену (например, `/settarget diesel 1.75`).
- `/settings`: Открыть меню настроек.
- `/backup`: (Только для администратора) Создать резервную копию в Google Drive.
- `/restore`: (Только для администратора) Восстановить данные из Google Drive.

## 🌐 Веб-панель

Веб-панель доступна по адресу `http://localhost:3000`. Она отображает:
- Общую статистику (количество станций, записей, пользователей).
- Список заправочных станций с актуальными ценами.
- Информацию о состоянии сервера и времени последней проверки.
- Поиск по станциям.

## 🗂️ Структура файлов

- `index.js`: Основной файл приложения (логика бота, веб-сервер, парсинг).
- `info.html`: HTML-шаблон для веб-панели.
- `css/main.css`: Стили для веб-панели.
- `database.json`: Хранит историю цен.
- `stations.json`: Список заправочных станций для отслеживания.
- `users.json`: Данные пользователей Telegram.
- `package.json`: Зависимости проекта.
- `render.yaml`: Конфигурация для развертывания на Render.
- `.gitignore`: Файлы, исключенные из Git.
- `README.md`: Этот файл.