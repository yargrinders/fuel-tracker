# 🐛 Отладка парсера - если цены не парсятся

## Проблема: бот работает, но цены не показываются

### Быстрая проверка:

1. **Смотри логи на Render.com:**
   ```
   Dashboard → твой сервис → Logs
   ```
   
   Должны быть строки вида:
   ```
   🔍 Парсинг станции 186650 - CLASSIC
     ✓ Diesel: 1.759€
     ✓ E5: 1.849€
     ✓ E10: 1.829€
   📊 Итого: Diesel=1.759, E5=1.849, E10=1.829
   ```

2. **Если видишь:**
   ```
   📊 Итого: Diesel=null, E5=null, E10=null
   ```
   → Парсер не находит цены на сайте (clever-tanken.de изменил структуру)

---

## 🔧 Решение: ручная настройка селекторов

### Шаг 1: Открой страницу заправки в браузере
```
https://www.clever-tanken.de/tankstelle_details/186650
```

### Шаг 2: Открой инструменты разработчика (F12)

### Шаг 3: Найди элемент с ценой Diesel
- Правый клик на цену → "Inspect" / "Исследовать"
- Посмотри структуру HTML

**Пример вариантов:**
```html
<!-- Вариант А: простой span -->
<span class="price-value">1,759 €</span>

<!-- Вариант Б: в таблице -->
<tr>
  <td>Diesel</td>
  <td class="price">1.759 €</td>
</tr>

<!-- Вариант В: с data-атрибутом -->
<div data-fuel="diesel" data-price="1.759">
  Diesel: 1,759 €
</div>
```

### Шаг 4: Обнови парсер в index.js

Найди функцию `fetchStationPrices` и добавь свои селекторы:

```javascript
// Твой вариант (примеры):

// Если цена в span с классом "price-value"
const dieselPrice = $('.price-value').first().text();

// Если в таблице
$('tr').each((i, row) => {
  const cells = $(row).find('td');
  if (cells.eq(0).text().includes('Diesel')) {
    prices.diesel = parseFloat(cells.eq(1).text().replace(',', '.'));
  }
});

// Если в div с data-fuel
const dieselDiv = $('[data-fuel="diesel"]');
prices.diesel = parseFloat(dieselDiv.attr('data-price'));
```

---

## 📝 Альтернатива: ручной ввод цен (временно)

Если парсер сломался, можешь временно вручную добавлять цены:

### Отредактируй `database.json`:
```json
{
  "https://www.clever-tanken.de/tankstelle_details/186650": [
    {
      "id": "186650",
      "name": "CLASSIC",
      "prices": {
        "diesel": 1.759,
        "e5": 1.849,
        "e10": 1.829
      },
      "timestamp": "2026-02-01T16:00:00.000Z"
    }
  ]
}
```

Теперь `/prices` покажет эти цены. Но это только временное решение!

---

## 🆘 Если совсем не получается

### Вариант 1: Используй другой источник
Вместо clever-tanken.de можно парсить:
- tankerkoenig.de (у них есть API!)
- benzinpreis-aktuell.de

### Вариант 2: Используй API Tankerkönig
```javascript
const API_KEY = 'твой_ключ'; // Получи на tankerkoenig.de
const url = `https://creativecommons.tankerkoenig.de/json/detail.php?id=186650&apikey=${API_KEY}`;
// API возвращает готовый JSON с ценами!
```

### Вариант 3: Попроси помощи
- Скопируй HTML код страницы заправки
- Отправь мне → я обновлю селекторы

---

## 📊 Проверка что всё работает

После исправлений:

1. **Git commit + push** → Render автоматически обновится

2. **Проверь логи** через 5 минут:
   ```
   Должны появиться строки с ценами:
   ✓ Diesel: 1.759€
   ```

3. **Telegram бот:**
   ```
   /check
   /prices
   
   Должны показаться цены!
   ```

---

## 🎯 Совет

Clever-tanken.de может блокировать частые запросы. Используй:
- `User-Agent` браузера (уже есть в коде)
- Задержки между запросами (уже 5 минут)
- Если блокируют → переключись на Tankerkönig API
